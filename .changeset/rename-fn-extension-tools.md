---
"@gsxdsm/fusion": patch
---

Rename pi extension tool labels and task ID examples from KB to fn/FN branding, slow the default peer sync cadence to reduce background dashboard load, automatically retry triage when a spec review is missing or not approved, fall back when a validator model rejects provider parameters, and recover stale in-review tasks with incomplete steps by sending them back to todo.
